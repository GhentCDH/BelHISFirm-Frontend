# Sampo UI Dockerfile
FROM node:18-alpine

WORKDIR /app

# Copy package files
# COPY package*.json ./

# Install dependencies
# RUN npm ci --only=production

# Copy application files
# COPY . .

# Build the application if needed
# RUN npm run build

# Expose port
EXPOSE 3000

# Start command
# CMD ["npm", "start"]

# Placeholder - Add your Sampo UI application files and uncomment the commands above
CMD ["sh", "-c", "echo 'Sampo UI placeholder - Add your application files' && tail -f /dev/null"]
